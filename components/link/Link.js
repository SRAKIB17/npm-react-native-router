import React, { useContext } from 'react'; import { ImageStyle, Pressable, TextStyle, ViewStyle } from 'react-native'; import StyledText from '../text/StyledText'; import { useRouter } from '../../navigators/router/RouterContext'; import { useTheme } from '../../navigators/theming/ThemeContext'; export default function Link({ href = '', children, style = {} }) { const { colors } = useTheme(); const router = useRouter(); return (<Pressable onPress={() => router.push(href)}><StyledText style={[{ color: colors.link }, style]}>{children}</StyledText></Pressable>); }