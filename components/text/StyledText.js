import React from 'react'; import { Text } from 'react-native'; import { global_styles } from '../global'; import { useTheme } from '../../navigators/theming/ThemeContext'; const StyledText = ({ children, style, numberOfLines }) => { const { colors } = useTheme(); return (<Text numberOfLines={numberOfLines} style={[global_styles.text_base, { color: colors.text }, style]} > {children} </Text>); }; export default StyledText; 